# r29.2 UX改善 検証記録（2026-09-24）

対象: 比較方式・税率シナリオによる行入力列、キー移動、選択案内、固定サマリー、結果文言。税額エンジン、制度判定、CSV解析、顧客出力の生成関数は変更していない。

| ID | 改修内容 | 主な変更箇所 | ブラウザー確認 | テスト結果 |
|---|---|---|---|---|
| U01 | 必要列だけ表示し、Enter・Tabの対象を統一。非表示値は元行に保持し現行計算から食品内数を除外 | `index.html` の行描画・集計用アダプター・列制御 | 公開版で一般のみ、簡易追加、食品1％を確認 | T01〜T04および行入力・税額回帰 |
| U02 | 複数選択の明示、選択済み印、すべて選択、初期案内と未選択操作後の警告を区別 | `index.html` の方式選択・ワークフロー | 初期案内・未選択押下・4方式全選択と警告解除を公開版で確認 | T05〜T06 |
| U03 | 表示列数別の最小幅、課税区分列を短縮し正式名称をtitle・候補に保持 | `index.html` の行入力CSS | 4幅で表の実幅と14px本文を測定 | T09のDOM・CSS検証 |
| U04 | 固定サマリーへ対象期・選択方式を表示。結果から方式選択へ戻れ、単独試算と限定比較を区別 | `index.html` のワークフロー・結果表示 | 単独結果500,000円と固定サマリーを確認 | T07〜T08 |

## 自動検証

- `npm test`: 347件成功・0件失敗。既存の本則500,000円、食品1％個別940,000円・差70,000円、区分52の943,000円、将来割合80〜0％、CSV置換・追加・再反映・キャンセルの期待値を維持。
- `npm run build`: 静的検証成功。
- `git diff --check`: 変更箇所の空白・パッチ形式に問題なし（Gitの改行コード警告のみ）。
- 新規 `tests/ux-comparison-methods.test.js`: 列の表示条件、Enter順序、元値保持、非表示セルのth/td整合、選択UI、単独・複数結果を確認。
- `tests/index-integration.test.js`: 非表示の不正な食品内数は現行計算を止めず、食品1％へ戻したときに元値を検証することを確認。

## 実ブラウザー・印刷

GitHub Pagesの実ブラウザーで、合成入力のみを使用した。初期状態は通常の案内で、方式未選択のまま主ボタンを押すと第1画面に留まり警告へ切り替わった。「すべて選択」で4方式が選択され警告は消えた。一般課税のみ・現行税率では、売上は「課税区分／税率／税込金額／操作」、仕入は食品列なしと読み上げられ、非表示のth・tdは表のアクセシビリティ構造から除外された。課税区分1からEnterで税率、税率からTabで税込金額へ移動した。簡易課税を追加すると事業区分列が現れ、Enterの次欄も事業区分へ変わった。第5種を入力して簡易を外すと列は消えるが値5は保持された。食品1％では売上・仕入の食品列が現れ、軽減8％の金額からEnterで食品内数へ移動した。食品内数500,000円を入力後、現行税率へ戻すと列は消えるが元値は保持された。

税込売上11,000,000円、税込仕入5,500,000円、非課税売上0円、免税事業者等仕入なしの合成例で、本則の方式表と単独結果はいずれも納付見込500,000円だった。単独結果には他方式との差額を表示しない。ブラウザーコンソールのerrorは0件。

| 幅 | 売上表の表示幅／内容幅 | 仕入表の表示幅／内容幅 | 判定 |
|---|---:|---:|---|
| 1280px | 1210／1210px | 1210／1210px | 横スクロールなし |
| 1024px | 959／959px | 959／959px | 横スクロールなし |
| 768px | 703／760px | 703／850px | 細幅の横スクロールを許容 |
| 390px | 329／760px | 329／850px | 細幅の横スクロールを許容 |

全幅で本文と表の計算済み文字サイズは14px。実ブラウザーでのTab・Enterは上記の箇所を確認したが、全セルの総当たりは未実施。CSVファイルの実ブラウザーアップロードと印刷PDFのページ数・7pt下限も未実施。自動テストによるCSV・印刷の回帰と区別する。スクリーンショットは再レビューZIPに含めていない。

## 残る制限

- 細幅では横スクロールを許容する。課税区分名の末尾が省略される場合も、titleとdatalistに正式名称を残す。
- 食品1％は未施行の政策試算であり、申告税額確定には用いない。
