# r29・F01〜F04検証記録（2026-09-23）

## 基準と範囲

- 作業開始時：`main`、HEAD `9f4b55e29935df09627312c53cdd9536c4d700b7`、作業ツリーはクリーン。既存テストは296件成功。
- 対象：現在の本番 `index.html`、`tax-entry-rows.js`、`tax-entry-csv.js`、`journal-csv.js`。添付ZIP内 `reference-only` の抜粋コードを本番へ上書きしていない。
- 食品1％は未施行制度の政策試算という既存前提を維持。制度基準日は実装日に合わせて更新しない。
- 金額は申告書段階の端数処理なしの国税・地方税込の概算。内部値は丸めず比較し、画面表示は円単位。

## 修正・期待値・実測値

| ID | 原因と主な変更 | 期待値 | 実測値・検証 |
|---|---|---|---|
| F01 | 食品1％でも用途別仕入税額を旧8％のまま使っていた。元の税込行、食品内数、価格前提、実績1％を一度だけ通す共通集計から、総額と3用途を作り本則へ渡す。旧保存形式の復元不能な用途は推定しない。 | T01：課税売上対応10,000円、非課税売上対応100,000円、共通対応100,000円、控除210,000円、個別対応控除60,000円、本則940,000円。現行870,000円との差70,000円。 | すべて一致（浮動小数誤差は1e-7円以内）。行入力では旧「入力値固定」注記を出さず、計算過程も課税売上対応10,000円。税込価格据置939,306.930693円、比例895,000円、全額790,000円、区分52・70％で943,000円も一致。 |
| F02 | CSV反映の説明は置換だが、従来は手入力行を暗黙に残した。置換を標準にし、追加は手入力行がある場合の明示選択。旧CSV行・実績・確認情報は再反映時に更新。既存入力時のみ金額比較と手修正行消失を確認。 | T03：初回11,000,000円、手入力11,000,000円から置換11,000,000円、追加22,000,000円、同一CSV再追加も22,000,000円。キャンセルは不変。 | すべて一致。正常CSVの初回反映に追加確認なし。旧形式の集計入力へ追加は許さず、既存の届出条件・調整額は保持。 |
| F03 | 将来期の免税事業者等仕入控除割合だけ変え、用途別税額を当期から流用していた。元の仕入行・用途から将来期ごとに総額と用途別額を同時再集計。 | T04：80％当期本則870,000円、70％次期は用途別課税売上対応70,000円・共通100,000円、控除対象総額270,000円、個別対応控除120,000円、本則880,000円。50％900,000円、30％920,000円、0％950,000円。 | すべて一致。境界期は共通の期間内日数按分割合を総額・用途別へ同時適用。旧形式の用途別内訳が復元できない将来個別対応は未算定。食品1％の4期最適化は引き続き停止。 |
| F04 | 対象期と手動控除割合の矛盾が赤い表示に留まり、税額・候補・4期へ伝わらなかった。元割合を維持したまま本則計算可否、候補、確認事項、出力へ連携。CSVは元取引日で割合を検証し、過年度CSVを予測へ利用する場合は試算対象期の割合へ別集計。 | T05：2027年手動80％は本則・4期累計を未算定、他方式の単独参考税額は維持。70％へ修正後は本則880,000円。2026-09-30の80％と2026-10-01の70％は共存。過年度80％実績を2027年へ使うと70％予測。 | すべて一致。元取引日と割合が矛盾したCSV、返品・調整で元取引未確認の行は未算定理由を示す。コピー・CSV・印刷用前提の生成関数にも理由を保持。 |

## 実行した検証

- `npm test`：開始時296/296成功、最終311/311成功（追加15件）。
- `npm run build`：`Static validation passed.`、終了コード0。
- `git diff --check`：空白エラーなし。GitのLF→CRLF警告は作業環境の改行設定に関するもので、差分チェックのエラーではない。
- 本番関数を読み込むVM統合テスト、行集計・CSV解析のモジュールテスト、保存復元・帳票テキストの既存テストを実行。参照ZIPの既知不具合assertを成功条件にしていない。
- ブラウザー操作・PDF印刷：未実施。デスクトップ操作ツールは初期化時に `failed to write kernel assets`（os error 3）。Chrome headlessはGPUプロセス異常で終了し、Edge headlessはPDFを生成できなかった。自動テスト／静的検証を実ブラウザーやPDFの証拠とは扱わない。画面の入力操作、保存→再読込、印刷のページ数・7pt下限は実機での再確認が必要。

最終再実行は `npm test` 311件成功、`npm run build` 成功、`git diff --check` 成功（改行警告のみ）。

## 未確認・留意点

- 実ブラウザーでのキーボード入力、通常／顧客向けPDFの実ページ数と表示は未確認。既存の印刷DOM・CSSテストは通しているが、PDFの実確認の代用にはならない。
- 過年度CSVの期間と割合は集計行ごとの元取引日範囲のみ保存し、CSV全文・取引先・勘定科目名は保存しない。元取引日不明または返品・調整が混在する免税仕入は原資料で確認するまで本則を未算定とする。
- 本ツールは申告税額の確定用ではない。食品1％の成立・施行や個別案件の適用判定はこの変更で確認したものではない。

## 根拠

- [国税庁：仕入税額控除の計算方法](https://www.nta.go.jp/taxes/shiraberu/taxanswer/shohi/6401.htm)
- [国税庁：インボイス制度の経過措置見直し](https://www.nta.go.jp/taxes/shiraberu/zeimokubetsu/shohi/keigenzeiritsu/invoice-review/index.htm)
