# UIレビュー対応の確認記録

## r23：計算過程の端数表示（2026-09-18）

表示関数だけを修正。例えば税込売上1,000,000円の10％税額は、変更前の「90,909.0909090909円」から「90,909円」へ短縮します。内部値1000000×10÷110はそのままで、カードの円表示にも変更はありません。金額は既存カードと同じMath.round、割合は通常2桁、時間・固定係数は必要な小数を維持します。未算定を0円にせず、負額の括弧と負のゼロの正規化は表示だけに適用します。

式は概算記号と領域ごとの注記を用い、表示内訳の合計差を埋める調整は追加していません。申告書端数処理は内部値に対する処理段階と単位を明記します。入力・保存・CSV・税額エンジン・適用判定・顧客出力は変更対象外です。

実ブラウザのローカルファイルアクセス制約は継続しており、PC／390pxの実画面・開閉・1クリックの再確認は未実施です。自動テストと同一データの表示・内部値比較の結果は完了報告を参照してください。

変更前の一時コピーとの照合では、計算・入力取得・法定端数・4期・費用・CSV・保存・顧客出力の20関数が完全一致。通常67,253.6791566753円（円表示67,254円）、法定端数適用67,100円、還付-36,839.65351616655円（還付表示36,840円）の同一入力3例は、中間値を含め変更前後で一致しました。税額・CSV・費用エンジン3ファイルのSHA256も一致しています。

## r23最終調整（2026-09-18）

受領した実行確認結果の残件2点に限定して対応しました。`renderWorkflow()`は、初期画面ではCSVパネルの直前にない場合だけ挿入し、金額・結果画面では親画面が変わった場合だけナビゲーションを移動します。blurによる金額の整形・検証とclickによる遷移は維持しています。詳細試算の説明から基本画面へ移動済みの「本則の課税売上割合・用途区分」を除きました。税額公式・丸め・入力項目は変更していません。

改修前は162件成功。追加の回帰テストで、同じ画面の再描画時にDOMを移動しないこと、画面切替時・初期画面の位置訂正時には必要な移動を行うことを確認します。最終件数は完了報告に記載します。

受領文書のChromium検証は外部レビューの結果であり、今回こちらで再実施した結果ではありません。ローカルファイルアクセスの制約により、非課税売上等・基準期間売上・特定期間売上・仕入額からの実クリック1回、Tab/Enter、戻る、CSV再反映後の操作を実ブラウザでは再確認できていません。別経路への置換で制約を回避せず、自動テストと実ブラウザ未確認を区別します。コミット・プッシュ・公開はしていません。

## r23：本則課税の基本条件（2026-09-17）

変更前は本則の方法・非課税売上等・用途別仕入税額が結果側の詳細試算にあり、通常試算は概算控除率を使用していました。変更後は最初の現在の申告方式の直下に方法設定、金額画面に非課税売上等と必要な用途別税額を配置します。現在の方式が本則なら設定を展開し、簡易等でも同じ場所の「設定する」で開けます。入力の複製、新しい独立ステップはありません。

詳細試算から本則の方法選択を切り離し、同じ詳細計算エンジンを使用します。旧概算率を計算に使わず、非課税売上等が空欄の場合は本則だけ未算定とし、他方式の独立計算は継続します。全額控除では用途別入力を要求せず、一括比例では非使用の用途別値を混入させません。CSVも詳細試算を自動オンにせず、選択方法を保持します。

数値照合（調整10,000円、売上税額800,000円、控除対象仕入税額600,000円）：課税売上割合80％の一括比例は330,000円。課税売上対応300,000円・共通対応200,000円の個別対応は350,000円。同じ売上税額・仕入税額で全額控除を満たす場合は210,000円。詳細試算のオン／オフでこれらは変わりません（申告書端数処理を明示変更した場合を除く）。5億円・95％境界、短期年換算、当期売上と基準期間の区別もテスト対象です。

ブラウザのローカルファイルアクセス制約のため、今回は実画面の前後画像、PC／狭い画面の寸法、ファイル選択・実印刷は未確認です。上記はコードと実関数を使う自動テストの確認であり、実ブラウザ・実職員の操作確認ではありません。実顧客資料の照合も未実施です。最終件数と静的検証は完了報告を参照してください。

## r22：6点統合修正（2026-09-17）

以下はコードと実行テストによる構成比較です。今回のブラウザ実画面・スクリーンショットによる比較ではありません。

| 画面 | 修正前（r21作業版） | 修正後（r22） |
|---|---|---|
| 初期 | CSV不要の冒頭案内、手入力優先の入口、見出し内に税率ブロック | 冒頭案内を削除。同じスタイルの手入力／CSVボタン。税率ブロックを見出し行の外へ移動 |
| 金額入力 | 食品確認は第1画面、基準期間等は詳細側 | 食品金額直後に確認状況。既存の基準期間売上・特定期間売上・給与等支払額を通常表示 |
| 結果 | 費用を含む4段階、候補不足の表現 | 3段階で税額へ直接進む。未入力・エラー・参考・比較可能・1方式・全対象外を区別 |
| 費用を開く | 独立した番号付きステップ | 税額結果の後半の任意折りたたみ。金額と費用付き出力を保持 |

既存3入力の移動のみで入力IDや必須回答は追加していません。食品の照合済みは有効な入力（明示0円を含む）又はCSV明示実績を要し、関連額・前提の変更では該当する確認を解除します。正常な保存値とCSV純額0円の元行は保持します。

- 改修前：既存141件成功。改修後：既存141件＋追加9件＝150件成功。
- `npm.cmd run build`：静的検証成功。`git diff --check`：エラーなし（WindowsのLF/CRLF警告のみ）。
- 手入力の直接遷移、CSVプレビュー・明示反映・遅延解析の保護、旧保存画面番号、入力先リンク、計算過程と顧客出力の分離を自動テストで検証。
- U01–U28をコード・既存／追加テストに照合。ただしU02・U03・U23の実寸表示、U05のネイティブファイル選択、U17の実ブラウザのスクロール／フォーカス、U26の実印刷を含む視覚・ネイティブ操作は未確認。
- ブラウザのローカルファイルアクセス制約により、今回のPC1366×768／幅390pxでの実画面、開閉・遷移のブラウザ確認、変更前後のスクリーンショットは未取得。DOM操作はテスト用環境の検証であり実ブラウザ検証の代替とはしません。
- 実職員の初見操作、実顧客資料との照合、公開後の確認は未実施。コミット・プッシュ・公開なし。

## r16の過去の確認記録（以下は今回の検証結果ではありません）

## 入力導線と表示密度

指示書に記載された旧画面の測定値は、初期43入力・縦3,232px、食品モード67入力・縦5,431px、詳細表示73入力・縦5,971px（1366×900）です。改修後、ローカルブラウザーの1280×720で実表示の入力欄を数えたところ、通常の初期画面は10入力・縦1,085px、食品モードの売上・仕入画面は21入力・縦1,821px、追加負担画面は8入力・縦997pxでした。ブラウザー幅が異なるため縦寸法は厳密な同条件比較ではありません。税理士確認・詳細試算は結果画面の折りたたみにあります。

## 操作・計算の確認

- 目的を食品1％切替にすると、政策試算の注意と4画面のナビゲーションが現れる。個人の試算年2027への変更は開始日・終了日を2027年の暦年へ連動する。
- 第2種の軽減8％売上1,080万円と内数の食品1％対象1,080万円、対応仕入540万円と食品内数540万円を入力すると、売上税額10万円、仕入税額5万円を表示する。食品の内数は別表へ二重入力しない。
- 追加報酬に「不明」を入力すると金銭収支は「費用反映前」、入力エラーと顧客用出力無効を表示する。0円へ修正すると無効文字列としては扱わず、ただし仕入控除未確認・適用条件未確認の出力は「所内検討用」のままにする。
- 税額差58万円、税込追加報酬11万円、B案に未反映の確認済み控除1万円、その他費用0円、時間を金額換算しない場合は金銭収支48万円。控除がB案に反映済みなら再加算しない。計算モジュールのテストで検証する。
- 旧保存値に見積基準のない追加報酬があれば旧値を示して再入力を促し、旧額を自動計算へ流さない。

## 未確認

- 実顧客の帳簿、届出、食品・新聞等の区分、追加報酬の控除可否はツールで自動確定しない。
- 1366×900の厳密な同条件画像比較、実紙・PDF印刷、GitHub Pages公開後の動作はこのローカル確認に含めない。
