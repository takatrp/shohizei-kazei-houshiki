# r29.1 再レビュー残件 R01〜R03 検証記録

作業日：2026-09-23。対象：`main`、開始時HEAD `9f4b55e29935df09627312c53cdd9536c4d700b7`。前回F01〜F04の未コミット・未追跡変更を保持して、その上に差分を追加した。この検証時点ではコミット・プッシュ・公開はしていない。制度基準日は変更していない。

## 修正と数値照合

| ID | 原因と修正箇所 | 期待値 | 本番関数経路の実測 |
|---|---|---|---|
| R01 | `tax-engine.js`の本則判定が非表示の旧`creditMode`を参照。`index.html`で現在の仕入金額・非課税売上・控除方法・用途整合から計算可否を作り、`calculateEligibility`へ渡した上で届出等の選択可否と合成。空欄仕入と明示0円を区別 | 本則200,000円、簡易500,000円、差300,000円。4期は各200,000円・累計800,000円 | すべて一致。旧設定が未確認・確認済・推計でも200,000円。仕入空欄は本則未算定、区分5の明示0円は1,000,000円。簡易の継続制限中は税額計算と当期の選択不可を分離。保存復元後も本則200,000円 |
| R02 | `index.html`の割合・元取引日検証が通常行だけを見ていた。別保存の明示1％CSV実績の区分52・62・72にも共通の期間別割合を適用。通常取引の不一致を本則未算定へ伝達 | 2027-05-01・80％は本則未算定、70％へ訂正すると943,000円 | 一致。誤80％の942,000円を本則候補・コピー・CSV・印刷から除外。区分62・72、2028-09-30/10-01の境界、返品相殺0円、保存復元とCSV再反映を検証。他方式の単独参考税額は維持 |
| R03 | `index.html`の過年度CSV換算は計算過程にだけ表示。保存済み元行と実際の計算結果から共通の前提を生成し、通常コピー・CSV・比較印刷・一般印刷へ渡した | 本則880,000円、元80％→対象期70％、控除対象仕入税額の換算差額−10,000円 | 一致。元取引日2026-05-01、対象期2026-10-01〜2027-09-30、日数加重、金額自体の期間按分・年換算なしを各出力で確認。元割合混在と手入力併存、2028年の274日70％＋92日50％、換算なし、差額0円も検証 |

R01の新規テストは、既存VMのDOM代替を使うが、制度判定は本番の`calculateEligibility`、版情報は本番`APP_META`、高額資産判定と4期経路最適化も本番関数を使用する。旧控除設定を確認済みにするテスト用操作はない。R02・R03も合成CSVを実際の解析・反映関数に通す。DOM代替テストは実ブラウザーやPDF視認の証明ではない。

## 実行結果

- 修正前：`npm test` 311/311成功、`npm run build`成功。添付の追加回帰4件は1成功・3失敗（R01〜R03を再現）。
- 修正後：`npm test` 327件成功・0失敗。R01 5件、R02 6件、R03 5件を通常スイートへ追加。`npm run build`の静的検証成功。添付の追加回帰は4件成功。`git diff --check`成功（GitのLF→CRLF警告のみ）。
- 既存F01〜F04の食品1％・用途別3区分・CSV置換／追加／再反映・4期の期待値も通常テストで成功。R01の本則200,000円と4期800,000円、R02の943,000円、R03の880,000円はこの作業の追加テストで照合。
- 保存復元はR01の行入力・旧設定未確認、R02の誤割合と訂正後、R03の過年度CSV元割合と対象期変更後の前提再生成を自動テストで確認。

## 操作・印刷の検証境界

`mcp__node_repl__js`によるWindowsブラウザー操作と`mcp__cua_repl.js`の両方を試みたが、いずれも`failed to write kernel assets: 指定されたパスが見つかりません。(os error 3)`で開始できなかった。実ブラウザーでの手入力・ファイル選択・クリック・ページ再読込、Chrome印刷プレビュー、A4 PDFの実ページ数・可読性・7pt以上は**未実施**。既存のDOM代替テストによるHTML出力確認を実ブラウザー確認と混同しない。前回の画像・PDFを今回の証拠として流用していない。

再レビュー時はローカルHTTPで`index.html`を開き、R01新規手入力の方式表・上部結果・4期、R02誤80％CSV→70％訂正・保存再読込、R03過年度CSVの通常コピー／CSV／A4帳票を確認する。印刷は計算過程を閉じた状態でも換算前提が出ること、長文時は7ptを下回らず複数ページを許容すること、顧客用帳票に所内情報やCSV個別明細が入らないことを確認する。

## 残る制限と再実行

食品1％は未施行の仮定シナリオであり、4期最適化は従来どおり未対応。返品・調整の元取引日が不明なら確認事項として残し、返品日から元割合を自動確定しない。旧集計形式では復元できない食品用途別額を推測しない。元CSV全文・実顧客資料・所内原価は今回の通常出力・レビューZIPに含めない。

再実行コマンド（リポジトリ直下）：`npm test`、`npm run build`、`git diff --check`。添付レビュー側の追加回帰は`SHOHIZEI_ROOT`を本リポジトリへ指定し、`reference/r29-review/review-additional-regression.test.cjs`を`node --test`で実行する。添付資料の旧コード・旧PDFは本番へ上書きしない。

## 2026-09-24：r29.1再レビューの保存割合追補

添付レビューで指摘された、CSV明示1％仕入の保存値70と計算用0.7の不一致を修正した。明示実績だけを対象とする共通の正規化関数を取込・復元・日付検証・集計で使う。手入力行の80等の契約は変えていない。復元した明細がすべて有効な場合、別保存の明示1％表示集計を明細から再構成する。不正な明細を0円扱いして再構成しない。仕入総額に影響する金額・割合の不正は本則の全額控除・個別対応・一括比例配分を未算定にし、用途だけ不明な場合は個別対応のみ未算定とする。

| 保存値・条件 | 個別対応 | 全額控除 | 一括比例配分 | 確認 |
|---|---:|---:|---:|---|
| 70、"70"、0.7、"0.7" | 943,000円 | 793,000円 | 896,500円 | 12組すべて一致。保存・再復元後も0.7で一致 |
| 2027年の80％、空欄・null・不正文字・範囲外 | 未算定 | 未算定 | 未算定 | 比較候補から除外し、理由をコピー・CSV・印刷前提へ表示 |
| 不正な明示1％仕入金額 | 未算定 | 未算定 | 未算定 | 明細を黙って落として税額を出さない |
| 控除割合と金額は有効、用途のみ不明 | 未算定 | 793,000円 | 896,500円 | 既知の仕入総額を使う方式は維持 |

正しい70％CSVを本番解析・反映し、合成保存値を本番`restoreState`へ渡して集計→判定→比較候補→通常コピー・CSV・印刷前提まで通す追加5件を`tests/r29-r02-saved-ratio.test.js`へ収録した。保存済み表示集計を意図的に変えた場合の再構成と二重計上がないことも検証した。既存のR02テストで52・62・72と日付境界を確認している。

- `npm test`：332件成功、0件失敗、skipなし（従来327件＋今回5件）。
- `npm run build`：静的検証成功。
- 前回添付の独立回帰：4件成功。今回レビューに記載された独立スクリプト本体は添付されていないため直接実行できず、同等要件を通常スイートへ追加した。
- `git diff --check`：成功（WindowsのLF→CRLF警告のみ）。
- ブラウザー：Windows操作ランタイムは`failed to write kernel assets: 指定されたパスが見つかりません。(os error 3)`で開始できず、修正後の実ブラウザー再読込・実PDF印刷は未実施。DOM代替テストをその証明として扱わない。
- この検証時点ではコミット・プッシュ・公開は未実施。実顧客CSV・検証用PDFは変更対象に含めていない。
