# r29.2 UX改善 検証記録（2026-09-24）

対象: 比較方式・税率シナリオによる行入力列、キー移動、選択案内、固定サマリー、結果文言。税額エンジン、制度判定、CSV解析、顧客出力の生成関数は変更していない。

| ID | 改修内容 | 主な変更箇所 | ブラウザー確認 | テスト結果 |
|---|---|---|---|---|
| U01 | 必要列だけ表示し、Enter・Tabの対象を統一。非表示値は元行に保持し現行計算から食品内数を除外 | `index.html` の行描画・集計用アダプター・列制御 | 未実施 | T01〜T04および行入力・税額回帰 |
| U02 | 複数選択の明示、選択済み印、すべて選択、初期案内と未選択操作後の警告を区別 | `index.html` の方式選択・ワークフロー | 未実施 | T05〜T06 |
| U03 | 表示列数別の最小幅、課税区分列を短縮し正式名称をtitle・候補に保持 | `index.html` の行入力CSS | 未実施 | T09のDOM・CSS検証 |
| U04 | 固定サマリーへ対象期・選択方式を表示。結果から方式選択へ戻れ、単独試算と限定比較を区別 | `index.html` のワークフロー・結果表示 | 未実施 | T07〜T08 |

## 自動検証

- `npm test`: 347件成功・0件失敗。既存の本則500,000円、食品1％個別940,000円・差70,000円、区分52の943,000円、将来割合80〜0％、CSV置換・追加・再反映・キャンセルの期待値を維持。
- `npm run build`: 静的検証成功。
- `git diff --check`: 変更箇所の空白・パッチ形式に問題なし（Gitの改行コード警告のみ）。
- 新規 `tests/ux-comparison-methods.test.js`: 列の表示条件、Enter順序、元値保持、非表示セルのth/td整合、選択UI、単独・複数結果を確認。
- `tests/index-integration.test.js`: 非表示の不正な食品内数は現行計算を止めず、食品1％へ戻したときに元値を検証することを確認。

## 実ブラウザー・印刷

公開URLへの反映後に実施可能な範囲を追記する。1280/1024/768/390pxの実表示、物理キーボードのEnter/Tab、印刷PDFのページ数・7pt下限は、自動テストだけでは確認済みとしない。

## 残る制限

- 細幅では横スクロールを許容する。課税区分名の末尾が省略される場合も、titleとdatalistに正式名称を残す。
- 食品1％は未施行の政策試算であり、申告税額確定には用いない。
